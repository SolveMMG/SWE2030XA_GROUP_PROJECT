name: CD

on:
  push:
    branches: [main]

# Deployment is manual for now.
# To enable: add RENDER_DEPLOY_HOOK_SERVER and RENDER_DEPLOY_HOOK_CLIENT
# to repo Settings → Secrets and variables → Actions, then restore the deploy steps.

jobs:
  placeholder:
    name: CD placeholder
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment hooks not configured yet — skipping."
